resources:
  - ../../base
  - service.yaml
configMapGenerator:
  - name: dpro-config
    literals:
      - APP_ENV=production
      - NEXT_PUBLIC_SITE_URL=https://dpro.mof.gov.my
      - PRODUCTION_URL={{ vars.PRODUCTION_URL }}
